const state = {
    items:[]
};
  
  const getters = {
      getProducts(state){
          return state.items;
      }
  };
  
  const actions = {
    //   addProducts(context){

    //   }
  };
  
  const mutations = {};
  
  export default {
    state,
    getters,
    actions,
    mutations,
  };
  